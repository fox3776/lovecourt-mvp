###############################
# Lovecourt MVP - Env Example #
###############################

# Dify service base url (no trailing slash)
# e.g. https://your-dify-host
DIFY_BASE_URL=

# Dify API key (server-side recommended; avoid committing real keys)
DIFY_API_KEY=

# Use local mocks for development (true/false)
USE_MOCK=false

# Base public path (keep default for uni-app)
BASE_URL=/

# WeChat Cloud env id (bind your project to this env)
# Example: cloud1-xxxxxxxxxxxxxxxx
WX_CLOUD_ENV_ID=

# Force cloud-only (disable direct HTTP fallback)
# Set to true in production if you must NOT expose direct API; default false
FORCE_CLOUD_ONLY=false
